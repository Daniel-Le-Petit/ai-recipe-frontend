// frontend/src/components/Pages/CreerRecetteIA.jsx
import React from 'react';

// Si votre composant est défini comme une fonction nommée :
function CreerRecetteIA({ preferences, handleChange, handleSubmit, loading, error, mockMode, setMockMode, navigate }) {
  // ... votre code du composant ...
  return (
    <div>
      {/* Contenu de la page Creer Recette IA */}
      <h2>Générer une nouvelle recette par IA</h2>
      {/* ... */}
    </div>
  );
}
export default CreerRecetteIA;
